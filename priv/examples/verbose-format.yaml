who: bob
where: OPS
priority: 3
why: |
  Traditional verbose format with separate 'where' field for backward compatibility.
  Some users prefer explicit field separation.
what: |
  - [ ] Investigate performance bottleneck
  - [ ] Implement optimization
  - [ ] Run benchmarks
  - [ ] Document findings
# Optional: specify cycle (number or name)
# cycle: 2
# cycle: "Sprint 42"
# Optional: specify dependencies using issue identifiers
# dependencies:
#   - NIN-70
#   - DAT-16
